apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{.Values.AzDProxy.prefixResourceDeployment}}-redis-deployment
  namespace: '{{ .Release.Namespace }}'
  labels:
  {{ include "common.labels" . | indent 4 }}
spec:
  selector:
    matchLabels:
      run: redis
  replicas: {{.Values.AzDProxy.cache.redisClient.replicas}}
  template:
    metadata:
      labels:
        run: redis
    spec:
      containers:
        # TODO : Add securityContext section.
        - name: {{.Values.AzDProxy.prefixResourceDeployment}}-redis
          image: {{.Values.AzDProxy.cache.redisClient.image}}
          imagePullPolicy: 'Always'
          # Configure Redis to use password
          args: ["--requirepass", "$(REDIS_PASS)"]
          # Direct redis to its config file
          command:
            - redis-server
            - "{{.Values.AzDProxy.cache.redisClient.mountPath}}/redis.conf"
          ports:
            - containerPort: {{.Values.AzDProxy.cache.redisClient.port}}
          volumeMounts:
            # redis config file
            - mountPath: {{.Values.AzDProxy.cache.redisClient.mountPath}}
              name: {{.Values.AzDProxy.configuration.volume.name}}
              readOnly: true
            # redis secret
            - mountPath: {{.Values.AzDProxy.cache.redisClient.secret.volume.mountPath | quote}}
              name: {{.Values.AzDProxy.cache.redisClient.secret.volume.name}}
              readOnly: true
          env:
          - name: REDIS_PASS
            valueFrom:
              secretKeyRef:
                name: {{.Values.AzDProxy.prefixResourceDeployment}}-redis-tls-certs
                key: REDIS_PASS
      volumes:
        - name: {{.Values.AzDProxy.cache.redisClient.secret.volume.name}}
          secret:
            secretName: {{.Values.AzDProxy.prefixResourceDeployment}}-redis-tls-certs
        - name: {{.Values.AzDProxy.configuration.volume.name}}
          configMap:
            name: {{.Values.AzDProxy.prefixResourceDeployment}}-config
            items:
            - key: redis-config
              path: redis.conf
